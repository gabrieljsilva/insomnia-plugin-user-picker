const getSelectedIdentifier = require('./get-selected-identifier')
const getSelectedPassword = require('./get-selected-password')
const getSelectedUser = require('./get-selected-name')

module.exports = {
  getSelectedIdentifier,
  getSelectedPassword,
  getSelectedUser
}
